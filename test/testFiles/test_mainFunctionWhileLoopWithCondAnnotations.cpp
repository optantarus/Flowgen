/*
* while loop is not shown even with conditional annotation
* - it is shown if variable is declared.
*/

int x;

int main()
{
    //$ [condition 1]
    while(x != 1)
    {
        //$ action 1
    }
}
